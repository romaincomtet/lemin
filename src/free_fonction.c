/*
** EPITECH PROJECT, 2019
** CPE_road_2019
** File description:
** free_fonction.c
*/

#include "lemin.h"

void free_all(road_t *D)
{
    // for (int i = 0; D->map[i] != NULL; i++) {
    //     free(D->map[i]);
    // }
    // free(D->map[D->cols]);
    // free(D->map);
    // free_structure2(D->open_list);
    // free_structure2(D->closed_list);
}

void free_structure(array_t *tmp)
{
    // free(tmp->S->position);
    // free(tmp->S);
    // free(tmp);
}

void free_solv(node_t *tmp)
{
    // free(tmp->position);
    // free(tmp);
}

void free_structure2(array_t *tmp)
{
    // array_t *tmp2;

    // while (tmp != NULL) {
    //     tmp2 = tmp;
    //     tmp = tmp->next;
    //     free(tmp2->S->position);
    //     free(tmp2->S);
    //     free(tmp2);
    // }
}